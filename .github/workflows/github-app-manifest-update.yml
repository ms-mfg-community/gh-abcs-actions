name: 03-2. GitHub App Manifest Update

on:
  workflow_dispatch:

# Placeholder: Complete Lab 3.3 to configure this workflow with your GitHub App
# This workflow demonstrates pushing to a protected branch using a GitHub App

jobs:
  update-manifest:
    name: Update Manifest (GitHub App)
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Complete Lab 3.3 to configure this workflow with your GitHub App"
